**This note is for Grok 4.6, and its brief is `EXTERNAL-REVIEW-7-PROMPT-GROK.md`.** If you are a
different model, or that file is not the one you were pointed at, stop and say so — a note pasted
into the wrong session is how round 5 was lost.

I maintain a small open-source repository of two Claude Code "skills" — structured markdown
instruction files that a coding agent loads and follows. One writes adversarial review prompts to
hand to a different AI model; the other adjudicates the reviews that come back. It is published CC0
on GitHub, it is unreleased in any product sense, and I own it entirely. Nothing here is a
production system and there is no user data anywhere near it.

**You reviewed this repository last round and returned 15 findings. All 15 were confirmed in whole
or part, and 27 changes were made in response. This round audits those changes.**

**Three things you should know before you start, because they change what to suspect.**

1. **You have a stake in this one.** The fixes you are auditing were written because of your
   findings. If one of them is wrong, or closed the wrong thing, that is the most valuable result
   you can return — and it is the result you are least likely to reach for. Treat your own round-6
   findings as a prior reviewer's claims, not as settled ground.

2. **The session that applied the fixes certified them with the tool it had just rewritten**, and
   then wrote the ledger saying they worked. It discloses this itself, at some length. **A
   disclosure is not a resolution**, and I would like to know whether the disclosure is complete.

3. **Last round's brief was written by that same session rather than by the skill**, so its
   questions were chosen by the party under audit — measured afterwards at 10 echoes, 3 partial and
   2 free out of 15. **This brief was generated by the skill instead.** Whether that actually
   changed anything is worth your scepticism too.

The full brief is on disk. Read it first — it pins the audit range, lists 20 load-bearing claims,
and states exactly what you may and may not touch:

**`EXTERNAL-REVIEW-7-PROMPT-GROK.md`** (repository root)

How to deliver it:

1. **Write your report to `EXTERNAL-REVIEW-7-GROK.md` in the repository root, creating it early and
   appending as you go** — not held in memory until the end. If the run gets cut short I would
   rather have half a report on disk than none. **That path is yours alone and is currently empty**;
   do not write to any other report file, whatever it is called.
2. **Return only a short summary at the end** — your coverage line, the ranked finding titles with
   impact levels, and the file path.

You may read anything in the repository **except `ROUND-6-HANDOFF.md`**, which is out of scope by
instruction — it carries a previous session's private doubts and reading it would contaminate this
run. You may run read-only commands, and **do anything you like under `/tmp`, including copying the
whole repository there and mutating it freely**. Please modify nothing else in the repository
itself. **Network: web search is allowed — cite the URLs for anything you source that way. No
installs.**

Two gotchas. The pytest fixtures under `calibration/cases/` write `__pycache__` if run in-tree —
copy to `/tmp` first. And the repository currently prints two `WARN disposition` lines about
`REVIEW-ADJUDICATION.md:2383` and `:2384`; those are deliberate and one of the claims is about
whether that decision was right, so engage them there rather than reporting them as new.

**There is a second reviewer this round**, working on `scripts/validate.py` from a different brief.
You are not reading its brief and it is not reading yours.

Please don't give me a ship/no-ship verdict — rank the findings by what it would cost to leave each
one unfixed instead. And if any two instructions in the brief contradict each other, or contradict
this note, report that as a process finding; you found one last time.
