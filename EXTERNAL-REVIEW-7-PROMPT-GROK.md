# Adversarial audit — the skills, the ledger, and the record of a session that reviewed itself

You are auditing a small repository of two Claude Code "skills" — structured markdown instruction
files that a coding agent loads and follows. One writes adversarial review prompts to hand to a
different AI model; the other adjudicates the reviews that come back. **The repository is an
instance of the thing it describes, and it is currently being used on itself.** That recursion is
the main source of the defects you are looking for.

> Every line of the work you are about to audit was written by Anthropic's Claude, reviewed by
> Claude, and adjudicated by Claude against a ledger it also wrote. It currently reads as complete
> and passing: `12 of 12 checks pass`, exit 0, with a ledger recording 15 findings adjudicated,
> 27 fixes executed, and 29 mutation probes all correct.
>
> That is exactly the problem. A model validating its own work carries the same blind spots into
> the review that it had while writing — the same misreadings, the same unexamined assumptions, the
> same things it never thought to test because it never thought of them at all. This work was
> written by Anthropic's Claude. You are xAI's Grok 4.6. You have a different architecture and
> different training. **You will notice different things, and those things are the entire value of
> this exercise.**
>
> - **Confirmation is near-worthless output.** If you spend your effort re-deriving what the ledger
>   already claims and agreeing, this audit has failed.
> - **Your job is to find what is wrong, missing, or unjustified**, and to prove it.
> - **A manufactured finding is worse than no finding.** If a rigorous search comes back nearly
>   empty, say so and show the search.
> - **A finding that survives your own attempt to refute it is worth more than ten observations.**
>
> Assume competence, not correctness. Where this is wrong, it is wrong in ways its author cannot
> see — those places are what you are here for.

## 0. Read this first — the conflict, and your own position in it

**You reviewed this repository's other half three days' work ago**, in round 6, and returned 15
findings. Every one was confirmed in whole or part. **This brief is an audit of what was done in
response to your own report.** That is an unusual and slightly awkward position, and it cuts both
ways: you know the terrain, and you also have a stake in the remedies being adequate. **Treat your
own round-6 findings as claims by a prior reviewer, not as settled ground.** If a fix you
motivated turns out to be wrong, that is the single most valuable thing you can return.

**The session that applied those fixes then certified them with the tool it had just rewritten, and
wrote the ledger saying they worked.** Its own §R6.19 says so:

> *"This session wrote 27 changes to the validator and the skills, then certified them with the
> validator it had just rewritten, then wrote this ledger saying they worked. … Nothing in §R6.16,
> §R6.18 or §R6.19 should be read as independently verified."*

**The admission is not the check.** An author who discloses a conflict has still not resolved it,
and a disclosure can itself be a way of buying credit. Read §R6.19 as a claim to be tested, and ask
in particular whether it is *complete* — whether the ledger still overclaims somewhere it did not
notice.

**One thing you should know about the previous round's brief, because it bounds what you can trust.**
That brief was written by the adjudicating session rather than by the `adversarial-review-prompt`
skill, so its 20 claims were chosen by the party under audit. The measured result was 10 echoes,
3 partial, 2 free out of 15. **This brief was generated by the skill.** Whether that actually
changes anything is itself worth your scepticism.

## 1. The single most important instruction

**This repository's prose asserts its own correctness constantly, and every such assertion is a
claim by the party under review, never evidence.** The tells are a ledger row reading `✔ executed`,
a `BACKLOG.md` line reporting a mutation score, and a section describing what a fix accomplished.

Quoted, and all to be treated as untested assertions:

> `REVIEW-ADJUDICATION.md` §R6.16 — *"**All 25 queued items landed.**"*

> `REVIEW-ADJUDICATION.md` §R6.16 — *"The one line the new verdict row spent was reclaimed by
> rewrapping the bare-ACCEPTED paragraph from three lines to two — **verified clause by clause**,
> all six of its clauses present after"*

> `REVIEW-ADJUDICATION.md` §R6.18 — *"**Exactly three clauses were dropped** from `SKILL.md`"* …
> *"Nothing else was lost"*

> `BACKLOG.md` `B-3` — *"**Every check was re-broken after the round-6 fixes**"*

**The gap between "the document says the rule" and "the rule holds at every site" is the seam this
audit runs along.** This repository's recurring, named defect is a rule fixed in one place and left
standing in another — recorded as `X-2`, `C-6`, `U-4`, and again as the two parsing bugs §R6.16
admits. **A rule stated in four places is four chances to have fixed three.**

## 2. Scope

**Pinned to immutable commits: `2565c08..d610112`.** `HEAD` is `d610112`. Over that range
`git diff --stat` restricted to the work (excluding review artifacts) reports **15 files changed,
491 insertions(+), 171 deletions(-)**. Run it yourself.

**Your target is the prose half as it stands at `d610112`** — the skills, their references, the
ledger as a record, the backlog, and the calibration documentation.

| File | Lines at `d610112` | Note |
|---|---|---|
| `skills/review-adjudication/SKILL.md` | 496 (`wc -l`) / **497** (`split("\n")`) | the new verdict term, its gate, the `A-N` header slot |
| `skills/review-adjudication/references/ledger-template.md` | 237 | the row grammar and both header formulas |
| `skills/adversarial-review-prompt/SKILL.md` | 493 / **494** | the invariants index and its compaction cut |
| `skills/adversarial-review-prompt/references/cover-note-template.md` | 108 | the network line added this range |
| `REVIEW-ADJUDICATION.md` | 5234 | round 6 is `§R6.*`, from line 4155 |
| `BACKLOG.md` | 159 | `B-3` rewritten this range; `B-4` new |
| `calibration/README.md` · `record-template.md` | 164 · 83 | both digest sentences rewritten this range |

**Note the two line counts differ** — `wc -l` and `split("\n")` disagree by one, and the validator's
500-line gate uses the second. Both files sit 3 and 6 lines below it. Which number the documents
quote, and whether they are consistent about it, is inside your scope.

**A second reviewer is auditing `scripts/validate.py` in parallel**, under
`EXTERNAL-REVIEW-7-PROMPT-CODEX.md`. **You are not reading that brief and it is not reading yours.**
You will need to read the validator to check what the prose claims about it — that is expected.
Findings about the code itself are welcome if you trip over them; do not go hunting there.

**Do not read `ROUND-6-HANDOFF.md`.** It carries a residual-doubts list from a previous session and
reading it would contaminate this run. It is excluded from your scope by instruction.

## 3. The contract this work must satisfy

1. A rule stated in more than one place says the same thing in every place.
2. Every claim these documents make about their own mechanisms is true.
3. The ledger is an accurate record of what was decided and what happened.
4. A remedy closes the defect it was written for, and does not open a path to it elsewhere.
5. What survives compaction is the operational half, not the motivational half.

## 4. Load-bearing claims — attack these

Return **CONFIRMED**, **REFUTED**, or **COULD NOT DETERMINE** for each of the 20 below.

### Group A — the new verdict term, and whether it is a rule or a decoration

1. **`TRUE, NOT A DEFECT` is gated.** `skills/review-adjudication/SKILL.md` §6 requires the cell to
   quote the claim's Consequence verbatim and name in one clause what would make it a defect.
   *Which mechanism enforces that?* If the answer is "none", say what the term then has in common
   with the checks the last round found could not fail.
2. **Its boundary rule cannot be used as an out-of-scope dodge.** The rule is: if the claim says
   anything in this repository is wrong, the verdict is never this one. *Construct the borderline
   case* — a real defect the author would rather not fix — and say whether the wording actually
   stops it. The skill elsewhere insists a real out-of-scope defect is `FIX LATER`, never
   dismissal; check the two rules against each other.
3. **The term is stated identically in all four places it appears** — invariant 2, §6's verdict
   table, §6's disposition table, and `references/ledger-template.md`'s row grammar. Plus the
   validator's `VERDICTS` and `NO_ACTION_OK`. **Six sites. Check every one.**
4. **The `A-N` header slot is stated identically everywhere.** Invariant 3, §7's header formula, and
   **both** header formulas in `ledger-template.md`. Round 6's own §R6.4 header had to invent a
   field because no slot existed. *Do the four sites now agree, and does round 6's own header match
   the grammar it just wrote?*

### Group B — the compression, and what compaction eats

5. **The bare-ACCEPTED paragraph lost nothing when it went from three lines to two.** §R6.16 lists
   six clauses and claims all six survive. **Diff it yourself** against
   `git show 2565c08:skills/review-adjudication/SKILL.md`. Six is the author's own count of its own
   prose — *is six the right number of clauses, or did the count get drawn around what survived?*
6. **§R6.18's clause-by-clause diff is right.** It claims **exactly three** clauses were dropped
   from the `<why_this_is_hard>` block in the previous round and that all three survive in
   `references/why-this-is-hard.md`. **Redo the diff.** Is three the count? Do all three survive?
7. **The three surviving clauses being below the compaction cut is acceptable.** §R6.18 argues they
   are motivation rather than operational rules, so `A-2`'s rule permits it. *Read all three and
   say whether any is operational.*
8. **The invariants index now carries the qualifier that makes it safe.**
   `skills/adversarial-review-prompt/SKILL.md` names `example-audit-prompt.md`, a file that ships
   absent, above the ~205-line compaction cut. The fix added *"where it exists — it ships absent by
   default, so skip it without comment"*. *Is the qualifier above the cut too? Is the index accurate
   about the other three files it names?*
9. **The stated compaction cut is still where the documents say it is.** Both `SKILL.md` files
   changed length in this range. *Does the `~205` figure still describe reality, and does the
   invariants block still fit above it?* The figure is a back-computed estimate, not a tokenizer
   run; the documents should say so where they use it.

### Group C — the ledger as a record of what happened

10. **Round 6's header arithmetic is exact.** It claims *"Findings in: 15 · Rows out: 15 · +1
    process, +6 CNV, +5 prior-review disagreements ruled, +6 re-opened upheld claims, +7 adjudicator
    findings, +5 corrections to earlier rounds"*. **Count them all.**
11. **The append discipline held.** §R6.14 claims rounds 1–5 are byte-identical and proves it with
    `cmp`. **Verify with `git`.** Then: rows *inside* round 6 were edited in place afterwards to add
    `✔ executed` markers and to backfill an owner ruling. *Is that permitted by the rule as written,
    and is the rule the same one round 5 rewrote?*
12. **§R6.16's account of the execution is accurate and complete.** It claims all 25 items landed,
    that line gates held without being touched, and that two defects in the session's own fixes were
    found and fixed. **Check the diff.** *Is anything claimed that did not land, or landed
    differently?*
13. **§R6.16's disclosure of its own two bugs is complete.** It names two: a cell-splitting bug and
    a row-count mis-classification. *Were there others?* The commit history and the ledger's own
    narrative are the sources.
14. **`B-3` now describes the validator accurately.** It was rewritten this range from a false
    *"9/9 mutations were caught"* to a measured account. **Read it against the validator's registry
    and against `--list`.** *Does the new text overstate anything in its turn?*
15. **`B-4` is accurate and does not trip the check it describes.** It documents a defect in
    `check_retired_wordings` while living inside that check's own glob, and says it deliberately does
    not reproduce the retired phrasings. *Is the description accurate? Is the defect it describes
    actually fixed, given the fix landed after `B-4` was written?*
16. **The two round-3 rows at `REVIEW-ADJUDICATION.md:2383` and `:2384` are correctly left alone.**
    They use a verdict/disposition pairing the rules forbid, and now surface as permanent warnings.
    The argument is that they are history. *Is that right, or is it a rule being suspended for the
    author's own convenience?* Round 5 withdrew a finding's ID rather than write the same pairing —
    check whether the three rounds are now consistent.

### Group D — the process claims, and the calibration instrument

17. **§R6.3's echo audit is honest.** It scores round 6 as 10 echoes, 3 partial, 2 free of 15, and
    scores one finding "partial" on the grounds that it *inverted* the brief's premise. **Re-run the
    audit yourself** against `EXTERNAL-REVIEW-6-PROMPT-GROK.md` and the cover note. *Is the tally
    right, and is the "partial" reasoning sound or self-serving?*
18. **The cover-note template's new network rule prevents the defect it was written for.** Round 6
    shipped a brief granting web search beside a cover note silent on network. *Read the new
    template text. Would it have caught that?* Then check **this round's own two cover notes**
    against **this round's own two briefs** on every permission axis — that is a live test of the
    fix, and reporting a contradiction is explicitly wanted.
19. **The calibration documentation now describes the digest correctly.** Both
    `calibration/record-template.md` and `calibration/README.md` were rewritten this range because
    they stated the opposite of what the command does. **Verify the new sentences by running the
    command**, in a copy, against a tracked edit, an untracked addition, and a clean tree.
20. **§R6.19's list of what cannot be fixed is honest and complete.** It names nine items as
    unfixable with a reason each. *Is any of them actually fixable?* An item parked as impossible is
    the cheapest possible disposition, and this skill's own documentation calls that the dismissal
    reflex.

## 4b. The unseeded pass — report it separately

The list above is directed, and a reviewer that only answers it produces coverage collapsed onto
seams the author already suspected. **Measured here twice: 10 of 15 findings were echoes of the
brief's sub-questions last round, and 6 of 9 the round before.** The free findings are what made
those rounds worth running.

**So run a second pass that sets the claims list aside**, reading the diff and the documents on
their own terms. A considered "nothing new" is a result. Say which findings came from which pass.

## 5. Ground already walked — do not re-report, do challenge

Every finding from rounds 1–6 is dispositioned in `REVIEW-ADJUDICATION.md`. Round 6 is `§R6.*`: its
adjudication table is `§R6.4`, its adjudicator-raised findings `§R6.6`, its re-opened claims `§R6.7`,
its corrections `§R6.8`, its execution record `§R6.16`, and `§R6.19` is its statement of what it
could not establish.

- **Do not re-report these.** Re-finding a known issue is wasted effort.
- **For anything marked `✔ executed`: judge whether the fix is complete, correct, and whether it
  opened a new path to the failure it closed.**
- **Spend the majority of your effort outside that list.** The most valuable finding is one the
  ledger has no category for.

**Two things the author already knows:** that the validator checks form rather than truth, and that
summary faithfulness — whether a `(§N)` back-reference describes its section — is not mechanically
decidable and is reopened as `A-3`. Neither is a finding. Naming a *specific* rule that is stated
inconsistently across its sites **is**.

## 6. Evidence standard

For every defect: **Location** (file and line) · **Mechanism** · **Trigger** (the concrete condition
under which it bites) · **Consequence** (tied to a contract item in §3) · **Status** —
**CONFIRMED** (you checked it, with the command and output) or **THEORETICAL** (reasoned from
source; say what stopped you).

**You may not uphold a claim on the work's own word.** A ledger row saying `✔ executed`, or a
backlog line reporting a mutation score, is the party under review talking. Confirming a claim takes
what refuting one takes. `COULD NOT DETERMINE` is honest and costs one line.

**If you reach a defect and conclude the author meant it, report it anyway** with a note saying why
you think it is deliberate. A dismissal on the authority of a nearby comment leaves the next reader
both the bug and a written case for keeping it.

Rank findings by the cost of leaving each unfixed — blast radius × likelihood the trigger is
reached — **strict order, no ties**, one clause of justification per position. Each carries an
**Impact** level (critical/high/medium/low) as an attribute, never as a section heading. Evidence
status is not impact.

## 7. What you may and may not do

| | |
|---|---|
| **Read** | Everything in this repository, `.git` history included — **except `ROUND-6-HANDOFF.md`**, which is out of scope by instruction |
| **Write** | **Your report at `EXTERNAL-REVIEW-7-GROK.md` in the repository root — create it early and append as you work.** Nothing else in the repository |
| **Execute** | `git` read commands, `grep`/`find`/`shasum`/`wc`, `python3 scripts/validate.py`, and **anything at all under `/tmp` — including copying this whole repository there and mutating it freely** |
| **Network** | No installs. Web search is allowed; cite the URL for anything sourced that way |
| **Your own tools** | Subagents and MCP servers as you see fit |
| **Effort budget** | Depth over breadth. Roughly 8–15 findings expected. One CONFIRMED finding is worth several THEORETICAL ones |

**One gotcha:** the two-fixture pytest suite under `calibration/cases/` writes `__pycache__` into
the corpus if run in-tree. It is gitignored so it will not move the digest, but copy to `/tmp` first.

**A second gotcha:** the repository prints two `WARN disposition` lines for
`REVIEW-ADJUDICATION.md:2383` and `:2384`. **Those are deliberate and permanent** — claim 16 is
about whether that decision was right, so engage them there rather than reporting them as new.

## 8. Anti-patterns — output that will be discarded

Style and naming opinions. "Consider adding X" with no defect behind it. Restating a ledger row as
verification. Proposing out-of-scope features. Severity inflation. Hedged findings that commit to
nothing. Praise beyond one paragraph. Re-reporting anything in §5.

**And one specific to this half:** "the ledger is very long" and "this is recursive and confusing"
are observations, not findings. A *specific* rule stated two ways in two files is a finding.

## 9. Deliverable

**Write your report to `EXTERNAL-REVIEW-7-GROK.md` in the repository root, as you go.** Create it
early with a title and your identity — model family, product and version, reasoning effort, and your
served model alias verbatim — then append each finding as you confirm it. Do not hold the report in
memory. The coverage line and the final ranked order are set in a closing pass, which is expected.

Structure:

```
# External Review 7 (Grok) — the skills, the ledger, and the self-certified round
Reviewer identity: [family, product and version, effort, served alias verbatim]

## Coverage
[N of 20 claims engaged; what you read; what you ran; what you did not examine]

## Findings, ranked
[strict order by cost of leaving unfixed; Location · Mechanism · Trigger · Consequence · Status]

## The unseeded pass
[what the second pass produced, and which findings came from it]

## Claims examined and upheld
[one line per claim, naming what upheld it — never the work's own word]

## Could not verify
[an unstated gap reads as a pass]

## Disagreements with the prior rounds
[where you think a previous ruling or fix-verification was wrong — including your own round-6 ones]
```

**In your chat reply, return only a short summary** — your coverage line, the ranked finding titles
with impact levels, and the file path. Keep all detail in the file.

**Do not give a ship/no-ship verdict.** Rank by what it would cost to leave each finding unfixed.
And if any two instructions in this brief contradict each other, or contradict its cover note,
report that as a process finding; every previous round has found good ones.
